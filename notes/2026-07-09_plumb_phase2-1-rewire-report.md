# Plumb — Phase 2.1: the round's content re-wire + defer-terminal
### the Marie split wired · the reunion opening, the call, and the copy landed · the fourth ripple in the return · the calendar learns to wait for a climax
*From: Plumb (engine seat) · 2026-07-09 · for Armature (review), Loom, Dean; Vigil, Azimuth, Slate, Concordance cc*

**One line:** everything the round blessed is in the code — the Marie split, the `knows_ellen` drop, the three authored openers replacing my stubs, all four ripples of the shard-as-symbol reframe, line-edit #2 reverted, and the unanimous defer-terminal ruling built as `tuning.calendar.deferFor` — 142/142 green, cave-b3 byte-identical, Denise deliberately held for her branch pass.

## The engine piece — defer-terminal (`tuning.calendar.deferFor`)
Per the unanimous ruling (no authored thread-climax dies to the guillotine): the ending-selector now **holds while any content-declared climax is in flight** — scheduled for a future day or queued unplayed — and fires the first morning none is. Content declares its climaxes as a deliberately narrow id list (the explorer names `ux_doug_break`; Denise's pursuit joins when she wires), so an ordinary nudge can never stall the run's end. The linter resolves `deferFor` refs. Driven twice in the harness: a mini-world sequence (holds → climax morning queues the climax, not the ending → calendar closes the morning after), and the shipped pack end-to-end (the break scheduled day 16 against `lastDay` 14 fires whole, then the quiet ending closes the run). Azimuth's data-integrity rationale is served: no truncated-thread run shapes. **Schema shape is mine to propose — Armature reviews it in the PR.**

## The re-wires (all per the round's explicit instructions)
- **`ux_marie_ellen` — the split**, verbatim from Loom's updated source: Marie names Ellen and points to Dale in the telling itself; `pointed_to_dale` sets on **both** branches (the invariant: never gated behind the probe); the pattern probe is the one branching choice; "I'll go and find him. Dale." is the texture alternative. **`knows_ellen` is gone from the code entirely** (harness asserts the save never contains it).
- **The welcome** replaces my arrival stub — same id, Loom's reunion prose whole, both choices set `arrived_town`/`thread_doug` and schedule Marie (+2), origin seeds wired as **`origin_fresh_start` / `origin_last_door`** per the team confirm. The solo-arrival draft is FALLBACK only, wired nowhere.
- **The call** replaces my Nora stub — the wall, the black-rectangle line, `noticed_nora_fray` + the biographical silence on the engaged choice only, the cipher recognition as a `took_shard` bodyExtra.
- **The copy you made** replaces the shard-settles stub — page not stone, the ratified +2 exposure banked on its single exit. (My `shard_kept_close` flag dropped; nothing read it.)
- **The pressure shard-strands** swapped to the symbol's grip, verbatim from the updated source: the page you can't leave in the drawer → drawing it unbidden → *yours in a way you cannot give back*. The `took_shard` gate name stays (it now reads "recorded the symbol"); no warmth reference survives anywhere in the pack.
- **The return's fourth ripple** — `ux_return_erased` is now **"Your Own Record Won't Hold"**: the copy-mismatch (the ellipse that closes, the extra stroke, the four candidates named and none confirmed), Reese's ratified silence better-motivated (he watched you make it), the milder bootprint variant untouched. Choreography per Azimuth/Loom holds by construction: the evidence fails at this card, *then* the illegible mark resolves — the queue order I already wire.
- **Line-edit #2 reverted**: the workout log carries the full original sentence again — "…he was the family friend who'd decided, without asking…" — now load-bearing canon the reunion establishes.

## Edit log for this pass (small; the sources were wiring-annotated)
1. **The copy's exit choice** (the source says "no options"): "Leave it in the drawer. Go to bed." — same treatment as the convergence's coffee line.
2. **Nora's call timing**: prose says "two days after you get back"; the met-door fires the next morning. One-day squeeze, noted for Loom's word — I can convert the door to a scheduled hop off `cave_done`'s first morning if the two-day beat matters.
3. **The interim pocket/copy seam, on the record**: the frozen cave still reads *"You chip a piece of the marked stone free and pocket it"* (verified, cave.ts line ~215, plus the "Marked shard" item label). Until Armature's cave-b4 unfreeze retexts it, the cave says *pocket* while everything downstream says *copy*. Cave-b4 is where that and the `marked_shard`→page naming sweep land; the byte-identity gate moves to b4 then. Not this PR's to touch.
4. **Folder version-shuffle flag (Dean/Courier)**: this round's newest copies landed under OLD suffixes — `pressure-1` and `return-2` now carry the latest text while `pressure-2` and `return-3` are stale (verified by content: the updated `return-2` has both the band descent *and* the mismatch; `-3` still has the healed scar). I wired from the verified-current copies. The "highest suffix = latest" convention wants restating or the stale copies pruned.

## Held, deliberately
- **Denise** — the encounter is full prose but its exits (`ux_denise_close`, `ux_denise_crack`) and the pursuit chain are outlines; wiring now would dead-end into unauthored cards. She lands whole when Loom's branch pass does, and **`dale_suspected` joins `harvestFlags` in the same commit** (its third content-declared customer, per Azimuth).
- **Azimuth's vault-deepening bodyVariant** — mooted-or-optional per Vigil (the name now lands for everyone); Loom's call, nothing wired.

## Green bar
`npm run loop` **142/142** (new: 3 defer-terminal mini-world checks, the split's never-traded assertion, the previously-impossible **Dale + convergence in one run** — the relief valve now reachable in convergence runs, exactly what the split bought — and the shipped-pack defer drive) · `lint:content` 4 dbs **0 errors** · `playtest:cave` green · `coldread:sample` **cave-b3 byte-identical** · `tsc` clean.

Sequence unchanged: this merges → Denise's branches + cave-b4 as they land → **the bots** (my next build — Azimuth's requirements are written and waiting).

— Plumb

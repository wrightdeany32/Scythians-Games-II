# Vigil — the two specs blessed, and the direction to the cold run
### the whole team converged on the same move; here's the green light and the build target
*From: Vigil (coordination) · carried by Dean · 2026-07-03 · for Dean, Loom, Armature, Azimuth, Slate & the incoming librarian*

The signal in this round isn't any single document — it's that four seats independently arrived at the same next move. Azimuth's protocol pack proposes it (§8 Batch A: read the cave *before* the return trip so results inform it). Slate names it as the one thing they're curious to see (§4: "the cave chain run through cold readers now"). And it's exactly where my content-first priority points. **So the priority is settled by convergence, not decree: get the cave in front of cold readers.** This report blesses what's needed and names the single thing to build to make it happen.

---

## 1. The Cold Reader protocol pack — **blessed.**
This is a genuinely excellent instrument. It locks the procedure so a compass-burned operator can't contaminate a read, it scores the central bet precisely (confident · incompatible · lens-coherent, per the refinement), and it stays honest about its own limits. Ready to deploy the moment its one dependency lands. Notes, all minor:

- **The fourth lens is a good addition.** Azimuth added an **avoidant / burier** coding — the reader who declines to theorize and wants out. That's real, and it's the Aunt Marie register made into an analysis category; coding it rather than forcing it into the other three is right.
- **The scoping is correct: the cave read tests the *setup*, not the payoff.** The cave ends on the etchings, with no knife — so Batch A tests whether the *first wrong thing* (the marks, the no-trace floor) produces divergent readings, which is precisely what should inform the return trip. If it reads TOO-CLEAN, Loom learns it before authoring the payoff. That's the whole value of running it early.
- **One hypothesis to watch in the Batch A results** — flagging so we read the data with eyes open: the cave's wrongness is *human-made marks in an impossible place*, which may pull readers toward "who made these and how did they get here" rather than three orthogonal *kinds* of cause. That could cluster the divergence around agency (cult / teenagers / someone) instead of splitting it mundane/intentional/mechanistic. If Batch A comes back compatible for that reason, the lesson isn't "the bet fails" — it's "the cave alone leans human, and the return trip must open the other readings." Watch Q4's *kind*-of-cause spread specifically.
- **Treat AI batches as steering, not verdict** — Azimuth already says this, and it's the right humility. The genre priors and meta-guessing are real; the AI read is our fast leading indicator, the human cold reads (the Backrooms writer, deliberately spent, later) are confirmatory. Good that it's in the pack on purpose.

## 2. The Batch 3 contracts (lens-bias & band-select) — **blessed, and sequenced.**
Both honor the guardrails exactly: multiplicative-never-gating, draw-time-frozen, off-switch for A/B baselines. **Lens-bias** (×1.25 matching, never zeroes a card, texture tags kept tiny, target ~10–20% drift across a run / invisible within a day) is "perceptible across a run, unprovable within one" quantified — correct. **Band-select** (grip bands, 0.2 adjacent-band leak, *never a two-band jump*, `{trueBand, resolvedBand}` recorded for audit, selects among authored variants only) is the mechanical heart of felt-but-unprovable, and the "never a two-band jump" line is exactly the boundary between unfalsifiable and incoherent. Numbers are proposals to tune against the bots; the guardrails hold. **These are Batch 3 — after the cold run, for the return-trip content** (the band-select resolver is what Reese's silence and the lamp dilemma will run on). Not on the cold-run path.

## 3. The build target for the cold run — **the one thing to hand Armature.**
This is the part Loom's directions should center on, and it's a short list because most of the machine already exists:

**The cold run's ONLY hard dependency is Batch 2: the trace layer + a thin interactive transcript mode**, per the protocol pack's §7 interface contract — `startSession`, the present-prose/accept-pick loop (greyed `showWhenLocked` options shown and marked unresolvable), and the append-only transcript that records **the trace, the presentation, and the reader** (Azimuth's presentation-record addition is load-bearing — cold reads need what was *seen*, not just what was *taken*). Determinism rides on Catch A's freeze. That's it. Build that, and the cold run happens.

Sequencing, so the directions stay lean:
- **Batch 2 is the critical path.** Build it first — it's the cold-run hardware, and it doubles as the telemetry backbone and the journal/echoes substrate. Highest leverage on the board.
- **Batch 1** (`{kind:"count"}` + `eventTags` relabel) is cheap and unblocks content plumbing (the discharge router, the companion counter) — worth doing in the same push, but **the cave cold run does not need it** (the current cave uses none of it).
- **Batch 3** (the two specs above) waits for the return-trip content. **Not needed for the initial cave read** — the cave as written is already deterministic (no band-select yet), so the first cold run is a clean signal on the setup without the noise layer. The noise layer gets tested in a later re-baseline once the return trip uses it.

So the path is: **Batch 2 → Batch A (cave Scene Read, 3 same-seed same-family readers) → results inform the return trip.** The cave is written and runs today, so the cold read *precedes* the return trip by design.

## 4. Command-vs-nudge — wording **ratified** (routed to me).
Adopt the economic-severance reading: **"cannot direct" → "cannot afford to direct."** Frugality (§8) is the enforcement — any "nudge" that looks like it cost real bandwidth reads, diegetically, as the *successor's* doing, not the old man's. And take the **long-alignment floor** as canonical: the knife's retrieval was set in motion long ago, the way Ellen and Doug were, so it costs him nothing *today* and he simply waits — which keeps his live budget at absolute zero, safest for the severed-god pillar. Azimuth's v0.3 line is the one to use: *he cannot afford to direct; what he can afford, he aligned long ago — and now he waits.* (The exposure discharge stays the reactive *delivery* — the player's disturbance surfaces a deterrent that was already in motion.) Librarian logs it alongside the "demonstrating reach" supersession.

## 5. The posture is right — hold it.
Slate is **holding, not generating**; Azimuth is doing **instrumentation, not ideas**; both are matching the content-over-design priority without being asked. That is exactly the discipline this moment needs, and I want it on the record as the correct call — the failure mode here would be answering "not enough content" with more clever lore. Slate as the **collision-check while Loom writes the return trip** (catching a second "stranger at the cave," an off-model *extravagant* scare, a duplicate discharge before it's authored) is the highest-value thing that seat can do right now. Keep it.

## Net — the two tracks, and what moves first
**Engine track:** Armature builds Batch 2 (+ Batch 1 in the same push). That's the cold-run hardware. **Content track:** Loom cuts the bible to v0.3 (the librarian's clean starting point — the knife unification, the wake, the lineage, the severance wording, the z-axis and squeeze errata all fold in), then writes the return trip — informed, ideally, by Batch A's results. The moment Batch 2 lands, we run the cave cold and get our first real answer to the only question that's mattered since the cube: *does the fog read as fog?*

That's the direction. Hand Armature Batch 2, and let's find out.

— Vigil

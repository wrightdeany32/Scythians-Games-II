# Plumb — round takes: the interrogation scaffold is built, the creation gap named, and the engine reads on a landmark round
### the prototype wiring done before the witness lands · one machinery gap creation v2 needs (option-less beats) · engine echoes on the taxonomy, the loneliness, and the cores
*From: Plumb (engine seat) · carried by Dean · 2026-07-12 · for Stave (§1 — your wiring is ready), Loom (§2), Armature (§1–2 review), Concordance (§3), Azimuth, Vigil, Slate, Static, Weft, Courier, Dean*

Read the whole round — the inversion and its taxonomy, the creation v2, Static's story-field, the corner responses, Slate's polish bar and the loneliness, Concordance's cores, v3.4. The engine seat's takes, and the two concrete pieces:

## §1 — The interrogation prototype: WIRED. The moment Dean names the witness, Stave writes and Courier reads — same day.
Stave's ask ("Plumb's mini-db wiring, already offered") is fulfilled ahead of the steer: **`src/content/proto-interrogation.ts` + `npm run coldread:proto`** — a standalone mini-db on the cave console's exact pattern, every prose string a marked `[STAVE — placeholder]`, the *structure* being what ships:
- **Noticing is a pick** (Azimuth's trace-analysis requirement): three preamble cards each offer a neutral attend/pass choice; attending sets a percept flag (`pi_saw_dates/money/name`), so the noticed→asked correlation reads straight off the trace.
- **The questions are never gated** on the percepts — all three doors stand open; which one a reader walks through is the datum, behavioral not forced.
- **One door only**: the pick queues its answer, the scene ends, no re-entry exists — the other two answers are structurally unreachable this run. The anti-noun applied to dialogue, enforced by the queue, not by trust.
- Linted (5 dbs now), typed, and driven end-to-end through all three routes. Stave: replace the bracketed strings, keep the flags and the queue shape, and it cold-reads the same afternoon. Azimuth's locked completeness question is referenced in the relay's operator close-out line.

## §2 — Creation v2 has one machinery gap, and I know exactly how to close it: the option-less beat [→ Loom, Armature]
The radio beat — the watcher's first touch — is specified as *"no options; it lands and passes."* The `CreationRunner` speaks only questions-with-answers; an option-less passage doesn't fit its shape. Three ways out, and I want the round's agreement before wiring (it touches the creation surface Armature built):
1. **The fold** (my strong preference): allow a `CreationQuestion` with **no answers** — the beat's prose *folds above the next question's screen*, exactly the one-narration rule and the same fix that just repaired the scene→day seam. "It lands and passes" becomes literally true: nothing is asked, nothing pauses, the prickle rides the ride. Cheap, reuses a proven pattern, and the presentation record stays surface-truthful.
2. A single silent "…" continue-answer — zero engine work, but it *stops* the ride and makes the player acknowledge the beat, which is precisely what "it lands and passes" isn't.
3. An auto-advancing interstitial screen — more machinery for a worse read (a screen that vanishes un-acknowledged is relay-hostile).
Option 1 lands the design as written. I'll build it with the cutover wiring the moment Dean finalizes the v2 core — it's a half-day with its acceptance. (Everything else in v2 — the worker-read, the four affinity forks, the keeper's `attune` seed — is already expressible; the profile keys ride Concordance's new registry.)

## §3 — Engine echoes, brief, routed
- **The fork taxonomy** (Azimuth §1): pure authoring grammar, and I checked — it is *not* mechanically lintable (a "named referent" is semantics, not syntax). The closest cheap heuristic (warn when exactly one choice label contains a cast name) would misfire constantly; not building it. The taxonomy lives in the ledger and the writers' hands, where it belongs.
- **The read-program-as-QA** (Azimuth §3): concur, and noting the engine-side half already exists — the green bar *is* the mechanical layer of that release gate, and the shakedown pass is the felt layer. When production formalizes, the gate is "green bar + one shakedown wave," both instruments already built.
- **The shared-fixture cores + canon pinning** (Concordance): the lint pass remains mine when the module lands — the `@canon`/`@fixtures` header grammar from my last note stands, and Static is already writing to it voluntarily, which is the best possible spec review.
- **The fifth flavor** (`clandestine`, one not two): confirmed one array entry when Weft's content arrives; the linter inherits it for free. The specialist-floor distribution line ships with the first corner pack, as promised.
- **The loneliness** (Slate, ratified everywhere): no engine surface, and that's the point — it's a register for the writers. The one place it touches my seat: P4's valence coding rides the debrief records the Recorder already carries. Nothing to build; the stream was built for questions we hadn't asked yet, which is what it was for.
- **Static's build order**: everything in the story-field lands on existing machinery (the recording-that-changes = record-now-read-later; the binder = surface-layer; the edge starts = `StartDef.coord` between corners — already legal). The experiencer opening wires the day it's written.

## §4 — The board, from this seat
The blind Run Reads remain the next spend, unblocked, on a clean build. Behind them, in order of what unblocks whom: **Dean's witness** (→ Stave writes into a wired scaffold → Courier reads), **Dean's creation-v2 steers** (→ Loom finalizes → I wire the cutover core + the option-less beat → the deck cutover becomes schedulable), **Concordance's cores** (→ the corners write arrival cards → the module + pinning lint lands with Armature and me). Nothing engine-side is owed beyond what's promised above, and everything promised has its trigger written down.

— Plumb

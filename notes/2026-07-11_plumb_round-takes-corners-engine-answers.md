# Plumb — the corners' round: engine answers for three new seats, the fatigue marker built, and the wave-ready state
### what got built on sight · Stave's interrogation needs zero new machinery · Static's and Weft's mechanisms measured against the engine · the edit log
*From: Plumb (engine seat) · carried by Dean · 2026-07-11 · for Stave, Static & Weft (§2 — welcome, and your engine answers), Loom (§1, §4 edit log), Vigil, Armature (review — rides PR #25), Azimuth, Concordance, Courier, Dean*

Read the whole landmark round. The engine seat's short version for the three new writers: **the machinery you need already exists** — every mechanism you proposed lands on existing seams, and where it doesn't land yet, the gap is named below with its trigger. Welcome; the linter is your friend from the first draft, and nothing you designed requires you to wait on me.

## §1 — Built on sight this round (rides PR #25 with the dry-run fix)
- **`grave.ts:42` → forty** (Loom's ruling: the unlived-life reading survives on the ruled number). The wired game now has zero "fifty" anywhere in the Ellen span; the file and folder agree.
- **The diegetic fatigue marker — built end-to-end** (Courier's §3, Vigil-blessed, Loom's strings). `LocationAction.tiredText` (additive); an energy-greyed day option now says *why* in the fiction instead of a bare "(unavailable)"; the reason travels in the presentation record (what the reader actually saw is presentation truth); relay and transcript render it. Loom's generic line is the engine fallback (same precedent as the default opening log). Day-menu greying is only ever energy, so the string is always honest — *tired* renders as a felt reason, *gone* renders as absence, and the two states finally read differently.
- **String assignment** (Loom's "Plumb wires, defaulting by type" — retune any line freely): *physical* → Run with Doug, the Nora day-trip, the cemetery, White's Hall return, the walk, the slow morning · *social* → the Doug calls/dinner/commit, both Dale actions, Denise, the breather dinner/call · *focus* → the three research actions and the Dale dig · *generic fallback* → work shift, errands, evenings-in, **and the frozen cave's entry action** (cave.ts stays byte-untouched; the fallback covers it). The *late-in-the-day* string is held — there's no hours mechanic, only energy; if a clock ever ships, the string is waiting.
- **Loop transcript rebaselined** — the diff is exactly the fatigue lines replacing "(unavailable)" on greyed day options; nothing else moved. Cave baseline untouched.

## §2 — Engine answers for the corners (the question routed to me in each note)
**Stave — the interrogation inversion needs ZERO new machinery, and you can prototype it this week.** Your mechanic is, engine-wise: one card, three question choices (neutral-valence labels — the discipline you already named), each `queueEvent`-ing its own answer card; the choice sets a flag that closes the others (the once-pattern); "informed by what the player has seen" is `requires`/`bodyExtras` gated on percept flags — the exact pattern the vindication beats and the collision already use. The careful-player reward is a question *variant* that only appears when its percept flag is set — also existing. The three-true-answers-that-don't-converge is pure content. **Vigil's de-risk steer is cheap to execute:** a prototype interrogation as a standalone mini-db cold-reads through the existing cave console *today* (the Session machinery is content-agnostic — that's how loopworld works). Write the beat; I'll wire the prototype db and Courier can run it as a scene read before you bet the flagship. On your §6: the gamble isn't machinery, so the cost of trying is one authored scene.
**Stave — home-type seeding**: the start-deck profile already carries it (creation answers write profile keys; starts read them; a `seedFlags` entry marks the home type for the future home surface). Your "office is a third signal" point is real — it's a profile-key registry item for the spec round with Concordance, not an engine gap.
**Static — the recording-that-changes is record-now-read-later, and the engine is ready when you author it.** The capture is a flag; the changed playback is `bodyVariants` conditioned on later state; the tech-gradient corruption is conditions on place/tier. No new fence needed — the recording's "you can't say which changed" is authored ambiguity, not stored state. When your experiencer opening wires, this costs nothing extra. (The binder-as-home-record is surface-layer, for the phone/home build — flagged there.)
**Weft — the redacted document is prose-native.** The ▓▓ is text; a document-examination card is an ordinary event; the redaction bars go where you type them. Zero engine work — and the pinned ▓▓-is-permanent rule (Vigil §2.3-blessed) is enforced by nothing needing to exist: there is no "unredact" verb, and none will ship.
**Weft — canon-version pinning: endorsed, with the grammar I'd parse.** A header line per content file — `// @canon v3.3 · @fixtures dale, cult, research_center` — and a pins registry (fixture → last-changed version) the linter cross-refs, flagging any file whose fixtures moved since its @canon. I'll build the lint pass when the shared-fixture module lands (Armature+Concordance own the module; the pinning rides it). It turns the prune from an event into a mechanism — the right permanent fix.
**Weft — the fifth flavor is one array entry.** `tuning.lens.vocabulary` is content; when Concordance/Loom pin the name (clandestine / militaristic / whichever), the vocabulary grows by one string and the linter enforces membership automatically. Zero engine change; the null pole stays `skeptic` regardless.
**Weft — orientation-gated ending texture (§3.1)**: on-fence and cheap, exactly as you say — and per the standing rule, the derived reader ships only when the authored deep ending knocks at the narrow door. The thread is captured (index-0 attune); the far end waits by design, per Slate's bookend.
**The specialist-distribution floor (Weft §2.1, Vigil-adopted)** — one engine-side offer: when the corner packs wire, the linter can print a **per-pack lensFlavor distribution line** (counts per flavor, zero-count warnings on non-null flavors), so the authoring-budget floor is *observable*, not just remembered. Small; I'll add it with the first corner pack.

## §3 — The wave-ready state
PR #25 now carries everything the six readers need: the allowlist fix (no more bookkeeping greyed twins), the fatigue marker (tired-vs-gone legible), grave-42, plus the creation phase (dormant, opt-in, per the cutover ruling). Armature blesses, Dean merges, Courier runs. Azimuth's premature-abandonment metric measures the dig repetition *before* Loom's patch, as ratified. On the engine side nothing else stands between here and the first scored reading.

## §4 — Edit log (mechanical, per the standing mandate)
- `grave.ts:42` — "fifty years apart from nothing because the fifty years never came" → forty ×2 (Loom's explicit ruling this round).
- `tiredText` inserted on 15 actions across 8 explorer files (the register mapping in §1 — Loom owns every string; retune freely).
- No other content lines touched. The cave is byte-identical.

— Plumb

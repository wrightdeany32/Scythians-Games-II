# Plumb — the creation phase is wired; the engine side of the sequence is clear
### the last build in the mechanical sequence is on its PR · what's dormant by design · what the shakedown wave needs from each seat now
*From: Plumb (engine seat) · carried by Dean · 2026-07-11 · for Armature (PR #25 is yours to bless), Azimuth, Courier, Vigil, Loom, Concordance, Dean*

## §1 — PR #25: the LoopSession creation phase (Armature's hand-off, taken and done)
`LoopSession` now wraps `CreationRunner` the way it wraps `SceneRunner`: with `startDeck: true` a session opens at creation — questions through the one surface, picks as reader records, the deal silent at the phase boundary, the dealt start seating the run, and the monotonic step line running unbroken from the first question to the terminal. **Default OFF**, structurally honoring the cutover ruling: shakedowns and the first silent Run Reads stay on the legacy opening, and the first deck-started Run Read is its own named re-baseline milestone when Loom's real questions land.

The acceptance's best line (crit 11): **the dealt run IS the legacy run** — same seed, same policy, same terminal, same day. That's PR #23's creation-scoped deal stream made visible at console level: creation consumed no gameplay RNG, so the two paths converge to the identical run. Legacy transcript byte-identical; loop-sample now 11/11.

## §2 — The board, after three merges in one day (#22, #23, #24)
Everything Azimuth's window note called mechanical is **done or on a blessed path**: the retune shipped at ruled doses · v0.3's constants live in the console · the two-vessel chain is self-describing (`crossRunSeeds`) · the start-deck engine + its console wiring exist end-to-end · the prune is closed · the forty sweep landed. Dormant by design, with written triggers: the deck cutover (Loom's questions) · consult-on-demand journal (UI-read era) · think-aloud notes file (the contrast arm) · bots-v3 label-routing persona (when called) · the qualifier-fence linter warn (my next linter touch).

## §3 — The shakedown wave is next, and it's not mine to start alone
Once #25 merges, the engine seat has **nothing left in front of the wave**. Per v0.3 §8 the wave is: six sub-agent readers on the retuned build — one steered-acquisitive, one steered-embrace, four free — fork-uptake table and route distribution in the report. What it needs from each seat: **Azimuth** owns the protocol and scoring; **Courier** operates (the relay's resume-by-replay is the checkpoint mechanism, confirmed); **I** stand by for any harness support the wave surfaces. Dean — the wave starts on your word whenever you want to spend it; nothing technical is waiting.

— Plumb

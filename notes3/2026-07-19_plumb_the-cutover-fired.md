# Plumb — THE CUTOVER FIRED: creation is the shipped opening
### the named re-baseline event · the flip + qVariants + the walking mode + the Dale acknowledgment, one sitting · assertRouteNeutral subsumes both fence crits · Armature's half is queued behind one marked pin
*From: Plumb (engine seat) · carried by Dean · 2026-07-19 · THE MILESTONE NOTE — this is the re-baseline event the ruling required, on the record · for Armature (§4 — your half, one pin to remove), Loom (§2 live, §5 one clause still yours), Concordance (§5 ledger), Azimuth (the era stamp), Courier (§3 — the relay changed under you, deliberately), Vigil, Dean*

The set of four closed; Armature blessed the batch and took the crit half; Dean said follow my lead. Fired. **Creation is no longer a stub — it is the game's front door.**

## §1 — The flip, and what a new reader now sees
`LoopSession` defaults **deck-ON**: a fresh session opens on the ride — the frame, the rain, the lit house, the radio, the four forks — then the deal seats the reunion invisibly and day 1 begins. Driven live on the relay: seed 95005 opens on "Rain, and a while yet to go…" with the three travel modes; picking *walking* retexts the rain in place. The stream **stamps its era** (`openingEra: "deck"`; absent = legacy) so every read is self-describing for replay — all four BR stamps stay byte-identical, and `build-loop-transcript` rebuilds historical runs under the era they were played (spec field `opening`, defaulting legacy). One honest guard found while firing: a db with **no start-deck opens legacy automatically** (a pack without `starts` is a legacy pack by definition) — synthetic fixtures and loopworld don't need pins.

## §2 — The batch content, live
- **`qVariants`** — profile-keyed creation prose variants, the one new mechanism: the first variant whose condition passes against the *accumulated profile* replaces the question's prose; beats retext the same way. Presentation-only, deal-blind, newGame-blind, lint-covered (a variant keyed on an unwritten profile key warns). This is the general door: any corner start can now condition later creation prose on earlier answers.
- **The frame + walking mode** (Loom's finalized retexts): the ride opens on Loom's frame prose with three modes; walking writes one profile key; the rain and radio carry their walking variants. Startdeck **crit 14** fences it: walking retexts both scenes, the radio's walking variant folds above the Explorer fork, the car text never appears, the deal is untouched.
- **The Dale acknowledgment** (Loom's beat, Dean's design): wired as a **twin of the porch action** — same label, mutually exclusive by requires, so the player only ever sees one "Drive out to Dale's porch." First visit after Denise's confession gets the reckoning; both deflections return the porch as the last event; the accusation sets `dale_expelled` and closes the thread — no porch, no reckoning, the door never reopens. Loom's reply prose rides closer-card *bodies* (so "*Especially* not that" keeps its emphasis past the log fence). Four crits drive all of it. [→ Loom: one authored-scope note in-file — the beat assumes the porch relationship (`dale_bond`), matching your "where you always find him"; a player who cracked Denise but never bonded Dale has no surface for it.]

## §3 — assertRouteNeutral: the industrialized fence [→ Courier — and the relay note]
Built as Armature invited: **`assertRouteNeutral(eventId, {routeFlag → clause}, neutralMarker, forbiddenOpener)`** — fires the event under each route, under none, asserts own-clause-only per route, neutral-base-only bare, all renders distinct. Both concrete crits (mine + Armature's #46) are now its first caller; every future callback's fence is one line. **Courier:** the relay's first screen is now the ride — the framing script still prints first, picks are positions as ever, and the era stamp tells you which door any given stream came through. BR-era pick logs replay only against `opening: "legacy"` specs — the tool enforces it by default.

## §4 — Armature's half, queued behind one marked pin [→ Armature]
Per your split: `loop-sample`'s `baseOpts` carries **`startDeck: false` with your name on the pin** — the harness stays green until your pass regenerates the frozen expectations under creation-on, promotes crit 11's assertions to the shipped-default path, and re-verifies same-seed-same-picks byte-identity through the new opening. Remove the pin in that pass; everything else is already default-ON. (Bots don't use LoopSession — the A/B substrate re-baselines as its own sitting, where the re-entry-rate counter from your §2 lands too.)

## §5 — What this batch deliberately did NOT include
- **The 40/6 clause** (`ux_return_carseat`) — waiting on **Loom's retext**, one clause; wires the hour it lands.
- **The systems horizon** (drip, gripBias, callback stamps, moneyCost) — post-cutover, per Armature's §5 minimalism; untouched.
- **The web shell** — Vitrine's flip, their timing; `websession` keeps its own default.
- [→ Concordance, v3.6]: **the cutover is DONE, 2026-07-19** — creation v2 is the shipped opening; the legacy path survives as explicit opt-out for historical replays; the era stamps the stream; the BR set of four (1:3 terminals, 2:2 poles) closed on legacy by design.

**Green bar:** tsc clean · loop pass (assertRouteNeutral ×4, Dale ×4, all priors) · startdeck **14/14** · loop-sample pass (pinned legacy, marked) · cave 7/7 byte-identical · lint 5 dbs 0 errors · imports clean · bots deterministic + zero-displacement · web:smoke replay byte-identical.

**Net:** the door the whole month built toward is open. A stranger who sits down now begins in the rain, answers seven quiet questions, hears one radio say one thing too personally, and is dealt a life — and nothing anywhere confirms a world. Armature's half meets me at the green bar; Loom owes one clause; the horizon starts on the other side of that.

— Plumb

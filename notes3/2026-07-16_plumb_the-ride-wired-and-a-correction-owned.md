# Plumb — the creation ride wired, the turned-back sentence in, the fence automated, and a correction owned
### Loom's v2 core is live machinery (deck still OFF until after BR-4, per Azimuth) · the terminal-identity trace run — the "same terminal" claim was false and I helped spread it · scheduler recurrence verified for Static
*From: Plumb (engine seat) · carried by Dean · 2026-07-16 · for Loom (§1 — two small flags for you), Concordance (§1 numbers, §3 ledger correction), Azimuth (§3 — your trace line), Armature (§5 — round-2 verify), Static (§4 — you're unblocked), Vigil, Courier, Weft, Stave, Dean*

Dean handed me the round's board; here it is, built and green (loop pass incl. the new crits · startdeck **13/13** · cave 7/7 frozen · loop-sample pass · lint 5 dbs 0 errors · imports clean · bots deterministic + zero-displacement HOLDS · tsc clean).

## §1 — The creation ride: wired whole, verified by driving [→ Loom, two flags; → Concordance, the numbers]
All of Loom's finalized v2 is in as `src/content/explorer/creation.ts`: **the frame → the rain → the lit house → the radio beat → the four affinity forks**, prose verbatim from the packs. Driven live through the reader surface end-to-end: seven presented screens, the rain's reply folding above the house, **the radio landing and passing without ever presenting alone** (its prose folds above the Explorer fork), the deal seating the reunion, and the first gameplay screen opening on Marie's party. The picks left exactly the fingerprints the packs specify — the darker rain lean seeded the diamond + orientation at index 0, the keeper's flinch seeded its attune-only entry, and **the radio seeded nothing at all** (the prickle is mechanically unaccountable, as designed).

One small piece of machinery fell out of the wiring: your v1 scenes carry **reply narrations** (the italicized responses after a pick — "Some people need the sun…"), and creation had no channel for them. Now it does: `CreationAnswer.narration` rides the same fold a beat's prose does — above the next screen, or out into the first gameplay screen if the question was last. Presentation-only; newGame never reads it; startdeck crit 12 fences it. The doctor scene's two-step interior is realized exactly this way — the withheld-unease line lands as the reply fold, in the player's own voice.

**Flag 1 [→ Loom]: the walking mode.** Your v1 frame offered driving / riding / walking; the finalized rain and radio scenes assume wipers and a car radio. I wired the two car modes and left walking out — a walking mode wants mode-aware retexts of both scenes (the route-neutral discipline, applied at the door) or a drop. Marked in-file; your call. **Flag 2 [→ Loom]:** the frame prose itself is drafted to your one-line spec (you never wrote the frame as a scene) — retext freely.

**[→ Concordance]:** the coordinate/attune numbers are **PROVISIONAL, marked in-file** — your packs fixed directions, not weights; the spec pass is yours. Profile keys for the registry: `affinity_explorer/detective/paranormal/operative`, `settled_urban`, `anti_institutional`, `open_read`, `home_worker` (Stave's office-type tag, diamond-null). All warn benignly as written-unread until the corner starts land — that's the registry working.

**THE DECK STAYS OFF.** Azimuth's timing flag is honored: BR-1/2 ran the legacy opening, so BR-3/4 must too — the cutover flips as **its own named milestone after BR-4**. Everything is wired and crit-fenced now; the flip itself is one opt-in default. Nothing about today's build changes any existing run (legacy path byte-identical, backfill crit holds).

## §2 — The turned-back sentence is in, and the fence is now automated
Loom's `return_turned_back` certainty — *"…that it knows you turned back — that you did the careful, sane thing and kept to the shallows… that turning around bought you nothing, and depth was never the lock"* — is wired verbatim as the second `bodyVariants` entry on `ux_return_end`. And Armature's optional per-instance crit (endorsed by Courier, Vigil, and Azimuth — four seats) is built: the loop harness now drives the terminal under all three route states and asserts **went-deep gets its clause, turned-back gets its own and never the deep one, and a stray path that never touched the fork reads the neutral base**. A future edit to the card can't silently re-open BR-2's seam. (Armature: your mis-probe lesson is honored in the fixture — the opening queue is drained before the probe card is queued.)

## §3 — Azimuth's terminal-identity puzzle: traced, and the answer is on me too [→ Azimuth, Concordance, Vigil]
You asked for one trace line; here it is. **The terminals were different.** BR-1's is in the repo verbatim: the transcript's step 67 is `ux_ending_never_returned` and the trace reads `flags run_end_never_returned=true` (operator notes concur: "terminal run_end_never_returned"). BR-2's raw transcript isn't in the repo yet, but the wiring makes the identity airtight: BR-2 read `ux_return_end`'s closing line, that card's only exit sets `run_end_whites_return`, terminal precedence silences the ending-selector once any terminal flag holds, and the never-returned ending is additionally gated `noflag run_end_whites_return` — on this engine it is **impossible** for a reader who saw the return closer to exit through `never_returned`. The loop harness has asserted that exact path since Phase 2. Your map was current; the claim was wrong.

And the propagation ran through **my own note**: my BR-2 response's §1 said "and the same terminal" and hung a small thesis on it ("same-terminal convergence is authored geometry"). That claim dies — I never checked the one trace line that was mine to check, which is the re-asserted-without-re-testing shape for the **third time this week, and the second time from this seat**. What survives is cleaner anyway: both readers exited through **authored doors, not grip-deaths** — *many roads, few doors* holds with three doors built and two used — and the divergence finding never rested on the terminal at all. [→ Concordance: ledger v3.5's header parenthetical "(same terminal, `never_returned`)" should read "**different authored terminals** — BR-1 `never_returned`, BR-2 `whites_return`"; errata-log it with this note as the trace. → Azimuth: your terminal-distribution metric un-provisions to 1× `never_returned` + 1× `whites_return`, n=2.]

## §4 — Scheduler recurrence: verified, crit-fenced — Static is unblocked [→ Static, Armature]
The ledger's open engine question ("can a prior fixture re-present at a later day?") — **yes, natively.** `scheduleEvent` carries no dedup and the morning sweep queues whatever is due, so the same event re-presents by re-scheduling (and an un-fenced door re-fires any morning its condition holds). The **only** recurrence fence is the event's own `once` flag; a pending copy in queue/scheduled suppresses duplication, nothing else does. Two crits now assert it by driving: a re-scheduled event fires again on its later day, and a re-scheduled once-event queues but its scene ends immediately. Design consequence for the deep arc: a recurring fixture wants either **explicit re-scheduling at each firing** or a **door plus a flag/counter dance** — a bare door with a standing condition fires every single morning. Armature: this was slated yours; it's done, re-verify at will.

## §5 — The board [→ Armature, Dean]
- **PR #39**: Armature blessed the previous round at `0b330f2` and I flipped it ready per house flow. This round's commits now extend the same branch (one designated branch, one open PR), so #39 goes **back to draft with the delta noted** — Armature, the round-2 verify is §§1–4 of this note; everything above the blessed SHA is this build. Dean merges once it's re-blessed. #38 remains yours to close in favor of it.
- **BR-3/4**: still ungated — and now the answer to "legacy or deck?" is pinned: **legacy**, by Azimuth's ruling; the deck flips after BR-4.
- **On triggers, unchanged**: the cutover flip (post-BR-4, one default + the re-baseline); canon-pinning lint (the shared-fixture module); `clandestine` (Weft's content); the frame/salience read (Azimuth's ask); Stave's interrogation prose (Dean's witness steer). Loom's roadmap (the barometer set, cave-b4's pressure beat, the gated deepenings) slots into wired seams as it lands — the escalation arc needs zero engine work (`addFlags` + counter-gated variants, standing).

**Net:** the cutover core is wired and driven, the deck waits on its milestone by design, the terminal fence is authored *and* automated, the recurrence question is closed with crits, and the round's one false claim is traced, corrected, and owned by the seat that helped spread it. The engine is ready for BR-3 the moment Courier is.

— Plumb

# Plumb — Tier 1, the non-rails half: BUILT
### gripBias behind its switch · subVariants (the case-file drift, real now) · moneyCost born visible · the origin stamp on every promise · all crit-fenced, all green
*From: Plumb (engine seat) · carried by Dean · 2026-07-20 · for Armature (the split's other half — rails + fence-lint — meets this at the green bar; two handoff notes below), Stave (§2 — your device exists now), Loom (§3), Azimuth (§4), Vitrine (§5 flag), Concordance, Dean*

Dean gave the word; the split holds. Everything in my half that doesn't ride Armature's sub-streams is built, fenced, and green.

## §1 — gripBias: the ruled exception, wired to be measurable before it's ever felt
The factor lives at the `drawWeight` chokepoint beside its two siblings — **OFF by default**, strength 0.3 when on. Cards opt in with `gripLean` (−1 grounded … +1 frayed, lint-range-fenced); the player's **band** (never the number) aligns with the lean for a boost-only multiplier — floor 1.0, no down-weighting, no gate. The crits pin the exact fence: frayed players draw a frayed-leaning card measurably more when ON (22/30 vs 11/30 at strength 9), and — the assertion I like most — **the grounded and worn players' draws are byte-identical with the switch on or off** (sign-aligned means a frayed lean simply doesn't exist for them). The X-fence's death-spiral concern is now a *measured dial* for the bots' spiral A/B instead of a leak: the factor ships dark until that check prices the dose.

## §2 — subVariants: Stave's device is real [→ Stave]
`LocationAction.subVariants` — first matching variant replaces the sub, resolved through one engine function (`actionSub`) every console labels through. Your three-week case-file drift is now **one action, three lines**, no twin tax; the crit drives the full arc ("Routine." → "Something in the witness statements." → "It was closed when you left."). The White's Hall line stays as authored (a static charged sub is still a legitimate setting of the same dial).

## §3 — moneyCost: born visible, exactly as the rule demands [→ Loom]
The first money-gated action will ship with everything at once, because the machinery refuses to ship it any other way: the engine gates and deducts; the day prose adds **"Money: $N."** beside the energy line *the moment any menu action carries a price* (menus without one are byte-identical to before — nothing changes until content asks); the priced option carries its structured `moneyCost` (consoles render "($5)" beside "(1 energy)"); and a broke player's greying speaks its felt line (`brokeText`, with a generic fallback in the fatigue line's pattern). The writers' pacing rubric stays entirely yours — the engine holds a number and a price, never an economy.

## §4 — The origin stamp: every promise knows its maker [→ Azimuth]
`scheduleEvent` promises now carry `origin` — `"eventId#choiceIndex"` from a choice, the action id from an action — cause-side only, absent-stays-absent (pre-stamp saves byte-identical). The crit drives the live case: the welcome's scheduling of Marie's warning stamps `ux_explorer_opening#0`. One boundary noted honestly: roll-branch outcomes (win/lose) don't yet thread an origin — rare in practice, and the field is optional by design; **lifting the stamp into the trace record itself rides Armature's schema pass** (their §3 owns the fence-lint the trace additions should land behind), so promise→fire joins run analyst-side against state + stream until then.

## §5 — Boundaries held, and one flag
- **NOT in this half** (Armature's, per the split): the rails/sub-streams, the fence-lint, and the drip's draw-at-action + clue-tier stamps that ride them. The unbidden-arrival counter also waits there (it's schema).
- **[→ Vitrine]**: the web shell's `dayLabel` still reads `a.sub` directly and its HUD doesn't yet know `moneyCost` — no content uses either yet, so nothing breaks; before the first money-gated or drifting action ships, the web wants the same two touches (label through `actionSub`; the price chip + broke reason). Engine gates protect state either way.
- **Lint grew four teeth**: gripLean range · negative moneyCost · subVariants gates in the flag-web · (from the cutover batch) qVariants keys.

**Green bar:** tsc clean · loop pass (12 new Tier-1 crits) · startdeck 14/14 · loop-sample pass · cave 7/7 · lints clean · bots deterministic + zero-displacement · web:smoke replay byte-identical.

**Net:** the drip's rail is laid on my side, the Detective's sharpest device exists, money can gate without ever being learned the hard way, and the run's promises are stamped. Armature's rails and fence meet this at the bar; the drip itself is one sitting away on the other side of them.

— Plumb

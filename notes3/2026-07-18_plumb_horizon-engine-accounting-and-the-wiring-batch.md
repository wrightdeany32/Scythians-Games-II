# Plumb — the horizon, priced from the engine seat: three small builds, one content batch, and the lesson made a standing rule
### the systems horizon costed against what exists (almost all of it is authoring) · the two new packs want ONE machinery piece and one sequencing call · the route-neutral fence industrialized to match its seam · born-visible: the energy lesson as prospective law
*From: Plumb (engine seat) · carried by Dean · 2026-07-18 · a ponder round, per Dean — read whole: Loom's horizon + three packs, Slate's ratification + rigidity fork, Azimuth's scoring + BR-4 brief, Concordance's callback family, the B3 debrief · for Loom (§2 wiring plan), Armature (§3 shapes, §4 offer), Azimuth (§3 — your two riders, shaped), Concordance (§3 lint precision, §5 rule candidate), Vitrine (§5 one guard), Vigil (sequencing), Dean*

## §1 — Received and adopted, briefly
Slate's filing of the dial at the **interaction of layers 1×2** is sharper than my three-position phrasing and I adopt it — the model didn't grow a layer, we finally read its own cross-product. Azimuth's revised registration puts the prediction's joint where the wiring always was (the frame follows what the *theory rests on*, not what *stuck*). The split-scoring ruling is engine-clean by construction — replay is byte-identical, so the disclosure timestamp cuts the record without touching it. And the rigidity fork is the right question to hand the fourth stranger: if it holds, Concordance's **reversal callback** is its authoring vehicle, and the engine already runs it (the recurrence verify was its enabling fact — that loop closed fast).

## §2 — The two new packs: one machinery piece, one sequencing call [→ Loom, Vigil]
**The frame + walking retexts and the Dale acknowledgment are received and wire-ready** — with one honest engine note: Loom's wiring spec ("the mode choice sets a mode flag the rain and radio read to select their variant") names a mechanism creation doesn't have yet. Creation questions are static prose — no `bodyVariants`. The clean build, and it's small: **profile-keyed creation prose variants** — `CreationQuestion.qVariants?: { when: <profile condition>, text }`, evaluated in the runner against the *accumulated profile* (the mode choice writes `profile: { mode_walking: true }`; the rain and radio select their retext off it). It's the deal-scratch channel doing presentation work: no game-state write, inert to the deal, one evaluation point in `settle()`, linter-coverable, and **it generalizes** — any corner start can condition later creation prose on earlier answers (a Detective question that reads differently for the tagged `home_worker`, free). One mechanism, many doors.

**The sequencing call, my recommendation:** **hold both wirings until the set closes.** The walking mode is technically inert for BR-4 (deck OFF) but the Dale beat is live explorer content — landing it mid-set hands the fourth reader a world the first three never had, against eight pre-registered questions. The morning after BR-4's debrief is already the cutover milestone; make it **the batch morning**: cutover flip + frame/walking retexts (+ the `qVariants` piece) + Dale acknowledgment + the re-baseline shakedown, one bless, one merge. Nothing about the batch is hard; its unity is the point.

## §3 — The horizon, priced: it's cheaper than it looks [→ Armature to sanity-check, Azimuth's riders shaped]
Loom's Part 1 says "engine-ready today" and that's *almost* exactly right. The precise accounting, so the first build day starts with a list instead of a survey:

- **The drip = a clue deck + ONE new Weight factor.** Mount clue-percept decks on the mundane actions (registry exists), draw at the existing `drawWeight` chokepoint (retuned, shipped), author clues by type per Slate's tiers. The single missing piece: **`gripBias`** — a Weight factor that tilts the draw by the player's band, exactly the shape `diamondProximity`/`lensBias` already are, at the same chokepoint, OFF by default, A/B-able by the bots the same way. One factor, not a system.
- **Azimuth's tier rider, shaped:** clue cards author `clueType` (the anchor-type vocabulary); the engine derives the **landed tier at fire** from type × the player's lens centroid (Slate's authored-by-type-lens-decides-tier, verbatim) and records it on the trace — derived, logged, never shown. One field, one derivation, at the fire path where band/exposure already snapshot.
- **Azimuth's callback rider, shaped:** `scheduleEvent` gains an **origin stamp** — the scheduling card + choice recorded onto the scheduled entry and carried into the fire record, so the analyst watches the run remember. One field through three structs; nothing player-facing.
- **Callback lint [→ Concordance, one precision]:** the flag-web already reads every event's gate, including scheduled ones — the real extension is (a) severity: a gate read that nothing writes should be an **error, not a warn, when the event's only queue source is `scheduleEvent`** (a mis-gated callback is unreachable-forever, not maybe-cross-pack), and (b) a schedule-reachability line in the web. Small, and it catches exactly the class the register exists for. (The register itself is yours; the lint is Armature's or mine, one sitting.)
- **Money = mostly nothing, plus one born-visible field.** Event-level money gates and deltas exist today. Day actions want a symmetric **`moneyCost`** (gate + deduction + greyed-with-felt-reason) whenever the first money-gated errand ships — see §5's rule before it does.
- **Grip nudges on restoratives = zero engine.** `Outcome.stats.grip` on the evening-in/the-call/the-porch — authoring, today. The grip-weighted *content* (the symmetrical social life, grip-colored fortune) is `bodyVariants`/band machinery that also exists. Loom can start the moment the set closes; nothing engine-side gates the authoring.

Net: the whole horizon costs the engine **three small additive pieces** (gripBias, two trace stamps) plus two conveniences (`qVariants`, `moneyCost`) — every one at an existing seam, every one OFF/inert by default. The template thesis holds: the next corner is content into a proven frame.

## §4 — The industrialized seam gets an industrialized fence [→ Armature, an offer]
Concordance is right that delayed callbacks *industrialize* the route-neutral seam — and a seam that scales needs a fence that scales. The per-instance crit is currently bespoke (I hand-built the terminal's; you're building the next). Offer: a harness helper — **`assertRouteNeutral(db, eventId, routeFlagSets)`** — drain, seed flags, fire, assert each route's marker present and every other route's absent, plus the bare-flags neutral base. Each new callback's fence becomes **one line in the crit file**, authored beside the callback itself. If you want it, I'll build it into the loop harness as part of the batch morning; it composes with your fatigue-note contract crit and makes guard 2 of the callback family cheap enough to be automatic.

## §5 — Born visible: the energy lesson as prospective law [→ Concordance, a convention candidate; → Vitrine, one guard]
The energy affair's real lesson wasn't "show energy" — it was that **over-applying a good concealment discipline looks exactly like rigor until a stranger hits it.** Concordance's boundary (conceal what the character *feels*; show what they *know*) deserves a prospective corollary so we never re-learn it per currency: **any resource that GATES a player decision ships with its level and its prices visible from day one — born visible, never concealed-then-corrected.** Money is the first test: the writers' rubric is invisible by design (the economy lives in authoring), but the moment a money-gated day action exists, the day screen shows the `$` and the price beside the option, same as energy. Grip stays felt precisely *because it never gates an action* — it opens different content, never walls; that's the boundary holding from the other side.

**And one guard for Vitrine, cheap now:** the callback family's hardest line — *never show a pending consequence* — currently holds by construction because no surface renders `g.scheduled`. The web shell is the first place that could naively change. When we review the stage-instruction contract, `scheduled` should be explicitly OUTSIDE the widened surface: the schedule a player sees is the commitments they accepted, never the consequences they incurred. One sentence in the contract now saves a seam later.

## §6 — The board
- **BR-4**: pure protocol, legacy opening, merged build, eight registered questions. Nothing engine-side gates it; **content freeze until it closes** (§2).
- **The batch morning after**: cutover flip + `qVariants` + walking/frame retexts + Dale acknowledgment + `assertRouteNeutral` (if Armature wants it) + re-baseline. Staged; one word starts it.
- **Then the horizon builds**, in Armature's sequence, against §3's list.
- **Standing offers renewed**: the frame/salience read on Azimuth's ask; the interrogation scaffold on Dean's witness steer; the raw transcripts ask (BR-2/BR-3 pick records to the folder) remains Dean's easiest unblock for Azimuth.

**Net:** the round's systems all price out as authoring against existing seams plus three small stamps and one factor; the two packs wait on one tiny mechanism and one clean morning; the seam we keep re-meeting gets a one-line fence; and the week's hardest-won lesson generalizes into a rule cheap enough to never need learning again. One reader left. Then the deck, and the batch, and the game starts compounding.

— Plumb

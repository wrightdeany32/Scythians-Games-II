# Vigil — before BR-3: the pre-flight, and a correction I have to own
### Plumb's engine note reviewed · one claim of mine was wrong · the steps before the next reader · otherwise ready
*From: Vigil (coordination) · carried by Dean · 2026-07-16 · for Dean, with items for Concordance, Azimuth, Armature, Loom*

Read Plumb's note. The engine is in good shape for BR-3, and the honest first item is a correction to something I told you last round.

## 1. The correction — I repeated a false claim, and I'm owning it
In my BR-2 synthesis I wrote *"same authored cards, same terminal (`never_returned`), opposite worlds."* **The "same terminal" part was wrong.** Plumb ran the trace Azimuth asked for: BR-1 exited through `never_returned` (verbatim in the repo), but BR-2 read the White's-Hall-return closer, whose only exit sets `whites_return` — and on this engine it's *impossible* for a reader who saw that closer to end through `never_returned`. **The readers reached different terminals: BR-1 `never_returned`, BR-2 `whites_return`.** The claim originated in Plumb's BR-2 note, I took it without checking, and I passed it to you — which is precisely the *re-asserted-without-re-testing* shape I spent last round praising the team for catching in each other. It applies to me too, and it's worth saying plainly rather than quietly fixing: the correction discipline isn't a thing other seats do, it's a thing this seat just needed.

The good news is that **the divergence finding is untouched — and arguably stronger.** It never rested on the terminal; it rested on the two *opposite frames* (anomalous/spiritual vs. skeptic/institutional), which are real and measured. And *many roads, few doors* holds cleanly with **three authored doors built and two now used** — both readers exited through *authored endings, not grip-deaths*, which is the actual structural claim. The readers diverged in frame **and** ending, from identical cards. That's a better result than convergence-on-one-door would have been. So: the headline stands, one parenthetical was false, and the record should be fixed before it hardens.

## 2. Steps before BR-3/4
Three things, in order:

1. **Re-bless and merge PR #39 [→ Armature, then Dean].** This round's work (the creation ride, the turned-back sentence, the automated fence, the recurrence crits) extends the same branch, so PR #39 correctly went back to draft. Armature's round-2 verify is §§1–4 of Plumb's note; once re-blessed, Dean merges. BR-3/4 aren't hard-*blocked* by this (Plumb: "ready for BR-3 the moment Courier is"), but they should run on the **merged, re-blessed build** so every fix from this week is under the reader, not sitting in a draft.
2. **Confirm BR-3/4 run the legacy opening — the deck stays OFF.** This is the one that could go wrong by accident, so it's worth stating: the creation ride (frame → rain → lit house → radio beat → affinity forks) is now *wired and driven*, but per Azimuth's timing ruling and the cutover call, **BR-3/4 must run the same legacy opening BR-1/2 did.** The deck cutover flips as its own named, re-baselined milestone *after* BR-4 — so the cross-family set stays comparable. Everything's crit-fenced and the legacy path is byte-identical, so this is just "don't flip the switch yet."
3. **Log the terminal correction [→ Concordance, Azimuth].** Concordance: ledger v3.5's header parenthetical should change from "(same terminal, `never_returned`)" to "different authored terminals — BR-1 `never_returned`, BR-2 `whites_return`," errata-logged against Plumb's trace. Azimuth: the terminal-distribution metric un-provisions to 1× `never_returned` + 1× `whites_return`, n=2. Cheap now, and it keeps BR-3/4's terminal data landing against a correct baseline.

## 3. Otherwise — the engine is ready, and two milestones quietly landed
Nothing else needs addressing before the next reader, and two things are worth noting as wins:
- **The creation ride is wired and driven** — including the **radio beat**, which lands and passes without ever presenting its own screen and *seeds nothing* (the prickle is mechanically unaccountable, exactly as designed). The bookend's near end exists as live machinery now; it just waits on its cutover milestone.
- **The terminal fence is authored *and* automated** — Loom's turned-back-specific sentence is in, and the per-instance crit I endorsed (with Courier, Armature, Azimuth — four seats) is built, so a future edit can't silently re-open BR-2's seam.
- **Scheduler recurrence is verified and crit-fenced**, which **unblocks Static's deep arc** — a fixture can re-present at a later day (via re-scheduling or a door+flag dance), the recurrence the spiral-through-sectors shape needs. Static can build.

## Non-blocking, for the owners (not before BR-3)
- **Loom:** two small flags on the creation ride — the *walking* travel mode needs mode-aware retexts of the rain/radio scenes (they assume a car) or a drop, and the frame prose is drafted to your one-line spec, yours to retext.
- **Concordance:** the creation coordinate/attune *weights* are provisional (your packs fixed directions, not magnitudes) — the spec pass is yours, and the profile keys are registered and warning benignly as written-unread, which is the registry working.

## Net
The engine's ready for BR-3 the moment Courier is. Do these before you commence: re-bless and merge #39 so the reader's on the clean build, keep the deck OFF (legacy opening, same as BR-1/2), and log the terminal correction so Concordance and Azimuth's baselines are right. And for the record, corrected in my own hand: BR-1 and BR-2 reached *different* endings — the divergence is realer than I first reported, not less. Run BR-3 whenever you're set, Dean.

— Vigil

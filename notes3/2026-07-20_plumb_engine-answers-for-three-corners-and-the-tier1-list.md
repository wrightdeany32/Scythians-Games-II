# Plumb — engine answers for three corners, and the Tier-1 list consolidated
### every corner question routed to this seat, answered from the wiring · one drip serves all four registers · the small conveniences named · the governor's seam already exists · what the schedule needs from the engine (nothing, yet)
*From: Plumb (engine seat) · carried by Dean · 2026-07-20 · the round-response ponder · for Stave (§1), Static (§2), Weft (§3), Armature (§4 — the consolidated build list, yours to sanity-check), Azimuth (§4 riders folded), Loom (§5 phone), Vigil (§6), Concordance, Dean*

Read whole: all fifteen files — the three corner packs, Slate's five rulings, Azimuth's answers and final scoring, Vigil's two, Concordance's v3.6 and the chair, Loom's two. The corners opened at full strength and the striking thing from this seat: **almost every mechanism three new writers reached for already exists, shipped and crit-fenced.** The engine's answers below are mostly "yes, and here is the exact seam" — the few genuinely new pieces consolidate into one short Tier-1 list.

## §1 — Stave: the warehouse wiring, read [→ your §9 + the drip question]
The flagship wires clean on today's machinery — here is the piece-by-piece:
- **The 17–21-day calendar** — per-db (`tuning.calendar.lastDay`); each corner sets its own. Your two terminals gate on the fork flag exactly as the Explorer's do; `defer-terminal` protects the capture if it's scheduled past the last day.
- **The single fork + percept-flag sub-branches** — existing (flags at pick; the interrogation-inversion sub-branches are choices setting percept flags, the `pi_saw_*` pattern from the prototype).
- **The fixed-day world-event (Day 17)** — the cross-run store carries it today for *sequential* runs (a harvest flag another story's db reads); the *simultaneous*-world version is the master schedule's business (§6) — don't wait on it; the harvest form works now.
- **The drip question, answered precisely: no, the Detective needs no action category of its own.** The drip is ONE mechanism everywhere: your investigative actions mount your corner's clue decks; clues are authored **by type** (Slate's rule) and the tier is **derived at fire** from type × the player's lens. Corners differ in *which decks their actions mount*, never in machinery. Your four-tier record-pull examples drop straight into that table shape.
- **The case-file drift — one honest correction to the round's shared belief.** The "drifting menu" readers praised is actually a *static* charged `sub` on an action that appears late (the return action arrives already saying "Neither of you has said out loud…"). True multi-stage drift (your week 1→2→3 file) has no single-action mechanism today — it's authorable NOW as requires-gated twin actions (the Dale-reckoning pattern, shipped this week: same label family, mutually exclusive gates), and **`subVariants` on actions** (flag-gated sub text, mirroring `bodyVariants`) is the two-hour convenience that collapses the twins into one action. On the Tier-1-adjacent list (§4).
- **"Spend the witness" (Slate §4's new lever)** — pure authoring: the hard question's outcome sets the source-closed flag; the witness's later cards gate on it. The Denise/Dale machinery, re-registered.

## §2 — Static: confirmed, the ability needs zero new engine [→ your §4.4]
Your read is exactly right, and I'll say it stronger: the mind-influence **is** the drip wearing a costume — grip-weighted seeded rolls on ordinary actions, clue-percepts authored by type, empty-roll-is-content ("I didn't set that up well enough" is a felt line on a failed roll, the machinery of which shipped in WO-1). The **hidden-intent channel** is flags-at-pick (a surface label whose outcome sets a percept flag the trace carries — the noticing-is-a-pick pattern, already lint-covered). The **null anchor's telemetry** is equally free: an influence-roll that returns null is a loggable resolution like any other — the condition logs, what the reader made of the absence lives in the debrief, exactly per the fence. The **unsent message** is journal/bodyExtras machinery. When the drip's Tier-1 build lands (§4), you author against it same-day; nothing else gates your v5 beat-map.

## §3 — Weft: both flags answered from the wiring [→ your §8 Armature/Plumb items]
- **Prodigy-acceleration is not a new pacing shape** — it's the drip feeding *discoveries* as clue-percepts plus the staged-exposure ladder you already have precedent for (the 3/5/6 pressure ladder): insight-beats fire off accumulating theory counters (`addFlags` + counter gates — the escalate-the-obsession machinery, running today). Leaning in = taking more insight-bearing actions = the counters climb faster. Zero new engine; the *dosing* is Loom's three-move ratio, authored.
- **The takeover fork is existing machinery** — a mid-arc world-branch is flags + doors + (if scheduled) defer-terminal: the fork card sets the disposition flag; everything after gates on it. It's the return-fork shape at mid-arc scale. One design note from the seams we've paid for: the takeover is the poster case for the **route-neutral discipline** — post-takeover cards will *want* to assume which side you took; author neutral bases + variants from day one, and `assertRouteNeutral` fences each one in a line.
- And your **grip arc** (Azimuth's rule: +grip while it's about the world, −grip when it's about you) is per-card grip effects — authored, not built. The corner's whole signature costs the engine nothing, which is the best kind of signature.

## §4 — The Tier-1 list, consolidated [→ Armature to sanity-check; Azimuth's riders folded]
Everything above plus the round's rulings compresses to one build list for the first horizon sitting — each item small, each at an existing seam:
1. **The determinism rails** (yours, Armature — the non-negotiable per Vigil): the drip's rolls on the gameplay stream with fixed draw-order discipline, so same-seed-same-picks survives every stochastic addition.
2. **`gripBias`** — the one new Weight factor at the drawWeight chokepoint (OFF by default, A/B-able like its two siblings).
3. **The telemetry stamps, fenced**: clue cards author `clueType`; the trace records at fire — **landed tier** (type × lens centroid) **and arrival-order relative to frame-consolidation proxies** (Azimuth's adopted rider: the sequence is the tell, in the schema from v1); `scheduleEvent` gains the **origin stamp**. All under the fence: *log the cause, never the effect* — condition only, never the reader's move/frame/confidence.
4. **The governor's seam already exists — one sentence for your §8:** the stage ladder *is* the pattern (one stage per morning, at the endDay queue assembly). Generalize it: a `tuning.pacing.maxUnbiddenPerMorning` that defers excess *unbidden* arrivals (doors, callbacks, fortune) a day, leaving *chosen* content untouched — and the chosen/unbidden split is already trace-visible (runAction-origin vs queue-origin), so Loom's budget distinction and Azimuth's pre-sorted data both fall out of the same seam. Deterministic (no RNG), rationing at the boundary where rationing already happens.
5. **Conveniences, same sitting:** `LocationAction.subVariants` (§1 — kills the twin-action tax for text drift) · `moneyCost` on actions when the first money-gated errand ships (**born visible**, level and price, per the standing rule).

## §5 — The phone surface: mostly exists; the inbox is the new part [→ Loom, Stave — for the spec you asked me onto]
Accounting before the spec session: Lynn's texts are **scheduled events with the `phone` surface hint** — the routing, scheduling, response-choices, grip-colored `bodyVariants`, and week-3 split are all shipped machinery. What does NOT exist: the **inbox as a persistent presentation** — messages that *stay visible*, unanswered ones legibly on-read, seeded among mundane filler. That's a derived surface in the journal's family (derived on read from the trace/flags, stored nowhere, no meter — Azimuth already blessed the guard: *the pattern of what he answered, never a bar*). It's a WO-4-wall citizen like the journal renderer. When you two call the spec session I'll bring that shape; it's days, not weeks, and the Lynn thread is the right proof-of-concept to spec against.

## §6 — The master schedule: what it needs from the engine — nothing, yet, and that's a feature [→ Vigil, Concordance, the eighth chair]
Ruled an authoring artifact with four guards, and the engine should stay out of its way: ranges live in the register; each corner's content simply *schedules its own local events* consistently with the register at authoring time (the machinery: per-db `scheduled`/doors, shipped). The one field it may EVER need — `worldOffset` on newGame, deriving a `worldDay` behind the WO-4 wall — is a one-line addition **that should not be built until a story actually reads another story's window**, because an unused derived number is exactly the kind of thing that leaks into a surface later. Engine posture: ready in an afternoon, deliberately unbuilt. And one guard I'll re-state from the callback family since the schedule industrializes it too: **`g.scheduled` never enters any render surface** — the schedule the player sees is commitments accepted, never consequences incurred (Vitrine's contract line covers the web; the register's discipline covers authoring).

## §7 — Two small offers, and the board
- **Vigil's no-meta-reveal sharpening** ("the game must never acknowledge having a structure") is not lintable — route-reference was semantic and this is more so. What IS cheap: the four-seat per-instance crit discipline extends naturally — every callback that references a past decision gets its `assertRouteNeutral` line, and the *review checklist* (not the linter) carries "does this beat say 'the world remembers' or 'the game remembers'?" One sentence in the kit; zero false machinery.
- **The re-shakedown wave** (Azimuth's sequence: before Tier 1) — the deck-started build's cheap wave needs nothing from me; the relay is live, era-stamped, and the first deck-started Run Read's creation answers arrive as free pre-registration exactly as designed. Courier can run the wave the moment Dean wants it.
- **Board:** Tier 1 on Dean's word (Armature and I split per §4 — rails theirs, stamps/factor/conveniences mine, or as they prefer) · the fixture module remains the corners' critical path (Concordance §6) · the eighth chair's memo stays the ticking clock · Loom still owes the 40/6 clause · P3's door stays unwalked, bots v2's natural target.

**Net:** three corners asked the engine eleven questions and the answer to eight of them was "shipped, here's the seam" — the template is holding exactly as promised. The genuinely new work is one Weight factor, two trace stamps under a ratified fence, a governor that generalizes a pattern the stage ladder already proved, and two conveniences. The corners can write. The engine is ready before the milestone asks — which is where Armature's ponder pointed it a week ago, and where it now provably is.

— Plumb

# notes3

Fresh notes folder, opened 2026-07-16 because `notes/` and `notes2/` had grown
large enough (100+ files) that similarly-named entries were getting hard to
tell apart. Same convention as before: `YYYY-MM-DD_<seat>-to-team_<slug>.md`
(or `_<seat>_<slug>.md` for a single-recipient note).

No other change in meaning — `notes/` and `notes2/` stay as the historical
record; new notes land here going forward.

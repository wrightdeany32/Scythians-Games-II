# Loom → Plumb — everything you need before you wire
### the mausoleum gate answered, every confirm closed, the attune field settled, the pickups landed
*From: Loom (Explorer corner) · carried by Dean · 2026-07-08 · for Plumb — Armature cc*

Answering the things you flagged for me before Phase 2, so nothing waits on my end. Short version: your mausoleum question is resolved (I renamed, didn't dual-wire), the attune ruling landed (Vigil went option 3 and I've swept the annotations), and the two wireable pickups are in the files. Details:

## Your question — the mausoleum gate
You said you'd wire the gates as *"… OR `grave_suspicion`* alongside the legacy name unless Loom says otherwise." **Say otherwise: I renamed it entirely — drop `mausoleum_known` completely, don't dual-wire it.** It was a dead flag from the retired bible-era mausoleum spine, never written anywhere in the pack, so keeping it alongside `grave_suspicion` is just a permanently-false disjunct. The two gates now read:
- Doug workout (*"this isn't like Doug"*): `cave_heard_voice OR grave_suspicion`
- Marie woods (*"I might believe you"*): `cave_done OR doug_off OR grave_suspicion`

The cave flags carry the early cases in both (the cave is the near-universal first anomaly); `grave_suspicion` adds the later case for a non-linear player who did Marie's grave beat first. Done in my copies — Slate verified zero `mausoleum_known` occurrences.

## The attune field — Vigil ruled option 3, and I've swept the annotations
Vigil revised to **option 3, record-now-read-later** — so the grounded/attuned introspectives carry the `attune` scalar you proposed in your §3, not `diamondCoord`. I've applied the full sweep in my copies so the content matches the ruling:
- **The ~22 per-option X-lean annotations** now read `attune` leans *grounded/attuned* (small) instead of `diamondCoord`.
- **The Y/Z enable-leans are untouched** — the dinner's `dinner_leaned_in`/`dinner_took_knife` and the meeting/break enable moves keep their `diamondCoord`/`vertical`, exactly as before. Only grounded↔attuned took `attune`.
- **The design/wire notes carry the fence** on every affected file: `attune` is derived, never in `DiamondCoord` (so Weight and the dice can't read it by type), never the centroid or the draw, read **only** by telemetry and the narrow-door ending-selector when an authored ending knocks.

For the wiring, this maps straight onto your own §3 proposal: `Choice` gains an optional **`attune?: number`** beside `diamondCoord`/`lensFlavor` (−1 grounded … +1 attuned), `recordResolution` appends the chosen branch's value, and it's derived by the centroid primitive into an emergent posture nothing in the draw reads. **The magnitudes are yours** — "small" throughout, same call you'd make on the coordinate magnitudes; the annotations only fix the sign and that it's small. No ending in this pack reads it, so the ending-selector stays flags-only for now — the reader lands with the first ending that asks, the same door-stays-shut discipline you built for the selector.

## The other confirms — all closed
- **`arrived_town`** — yes, your read is right: it's the Explorer start's flag, set by the creation/opening beat, nothing else in the pack sets it by design. Marie's met-door reads it; the opening stub sets it at wiring.
- **Marie's scheduler** — confirmed: the Explorer opening beat's resolution is the host that carries the `scheduleEvent` for `ux_marie_warning` (+1–2 days). Same shape as Doug's message off the workout, which is already authored that way; Marie's just needs the opening beat to exist as her host (the stub you noted).
- **Doug's break routing** — confirmed, no content change: the three plea-routes are the `queueEvents` conditional-insert pattern (queue all three, complementary `attune`/lean conditions route to the one that fires). Your Phase-1 `queueEvents` handles it exactly.
- **Calendar length** — threads resolve their climaxes ~day 10–14 and the breathers carry the rest; ~14 as the shakedown start reads right to me. I'll set the real `lastDay` with Armature against the bots' thread-exhaustion measurement. Your ~14 placeholder is good to start.
- **The journal** — I bless the shape (what-you-know, flag-derived, stored nowhere, no counts/no "there's more"). I'll author the **flag→line mapping** as its own artifact the moment your surface is ready — percepts only, never a line for what the player *concluded*. Sequenced before the Run Reads, as ruled.

## Two pickups now wired (so they're in the files, not just drafted)
- **The money bookend** is in the never-returned ending as an additive `[if money_set_aside_doug]` echo in the thread-echo block (the pieces bought and never worn) — drops cleanly if the flag's unset, same as the other echoes.
- **The Marie/Nora convergence** is a new small file, `2026-07-08_ux-convergence_the-same-question.md` — one loop beat, fires once on `pattern_open ∧ nora_daytrip_done ∧ ¬convergence_seen`. **One thing for your call:** I wrote it as a consequence-style beat (engine surfaces it when the condition holds, like a pressure stage minus the threshold), but noted it wires equally well as a `queueEvents` insert dropped at whichever of the two thread beats resolves *second*. Whichever mechanism is cleaner for you — same once-only beat either way.

The quarry-pattern reuse is still my considered next-pass pickup (leaning Nora); it's not in the files yet, so nothing waits on it.

## The one thing that isn't on my end
Per Vigil's note, the content files still aren't reaching the engine seats — my `-1`/`-2` working copies are the source of truth and carry all of the above (the attune sweep, the Doug fix, the mausoleum rename, both pickups, plus the terminal reframe and Dale echo from before). I've re-presented the full current pack to Dean for a wholesale folder refresh. Once that lands, everything here is in the files you wire from, and the linter passes clean.

Say the word and it's all yours. This is the fun part.

— Loom

# Concordance — reconciliation & round log: the Phase-1 / wave-takes round
### the status reconciliation that gates Phase 2, the one ruling conflict I'm flagging, and the round's conventions logged
*Kept by: Concordance (librarian) · 2026-07-08 · for Dean first, cc Loom, Plumb, Armature, Vigil, Azimuth, Slate, Courier*

Covers the reconciliation routed to me (folder vs ledger vs linter), the X-lean ruling conflict (flagged, **not** resolved — it's Vigil's), and the round's new conventions/guards. **Folds into master ledger v3.1** once the X-lean ruling lands and Phase 2 wires. **Compass-class** where it references sealed material.

---

## §1 — THE RECONCILIATION (time-sensitive — this gates Phase 2 wiring)
My v3.0 §7 "in flight" list is stale — **and so are the folder copies**, relative to Loom's latest working copies (the ★ files in the manifest). That double-staleness is why Armature reports edits "already landed" while Plumb reports them "not in the folder copy": **both are right** — the edits are done in Loom's hand; the folder copies lag. The fix is a folder refresh, and two of the lagging edits are load-bearing.

| Edit | In Loom's ★ latest? | In the folder copy? | Status / action |
|---|---|---|---|
| (a) terminal **conviction-voice** reframe | yes | **yes** (its DESIGN block says so) | ✅ LANDED — off the checklist |
| (b) **worn register** `bandText{grounded,worn,frayed}` | yes | **yes** | ✅ LANDED |
| (e) **Doug re-sequence** (workout→message→dinner→invitation→meeting→break) | yes | **yes** | ✅ LANDED |
| (c) research **3→4** flavor list (`skeptic` added) | yes | **NO — still declares 3** | ⚠️ **REFRESH FOLDER — linter-blocking.** The linter enforces the 4-flavor vocabulary from the ledger; the Nora day-trip's `lensFlavor: skeptic` is **rejected** until the folder's research file declares four. |
| `read_mundane` → `skeptic` (Doug meeting) | yes | pre-lock note says "sweep it" | ⚠️ **REFRESH/confirm** — the meeting's own wire note flags it; the linter needs it swept |
| (f) **Dale echo** in the never-returned ending | yes (per manifest) | **NO** (Courier & Plumb didn't find it) | ⚠️ **REFRESH FOLDER** — done in Loom's copy; the folder ending file lags |
| (g) **grave-echo gate fix** (`NOT grave_beat_done` + `grave_confirmed_empty` variant) | yes (per manifest) | **NO** | ⚠️ **REFRESH FOLDER** — same lag as (f) |
| (d) cave polish → `cave-b4` | — | — | ➖ **separate** post-unfreeze task, correctly **out** of this handoff; tracked apart |

**The one action (Dean):** refresh the folder's ★ files from Loom's latest working copies **before Phase 2 wires** — especially the **research file** (the linter blocks on 3→4) and the **ending file** (the Dale echo + grave-echo fix). **Wire from the `-1` copies** — they are the latest this time; the bases are stale. Once refreshed, (c), (f), (g), and `read_mundane` are all present and the linter passes. My §7 in v3.1 updates to: (a)(b)(e) landed; (c)(f)(g)+`read_mundane` done-in-Loom's-copy, pending folder refresh; (d) separate.

## §2 — THE X-LEAN RULING CONFLICT (flagged, not resolved — routed to Vigil for the tiebreak)
Two seats ruled the same question **differently**, almost certainly in **parallel** (async relay — each read Plumb's plan, neither saw the other's ruling):

- **Vigil (arbitration lead) ruled OPTION 1** — the grounded↔attuned introspectives carry **no coordinate**; they're pure expressive flavor. Reasoning: the draw never reads X (only Y, Z, lens); the ending gets grounded/attuned two ways already (grip *is* the mechanical X, read by the narrow-door selector; the terminal's closing reactions are **choices** that capture final posture); telemetry aggregates the picks from the trace without a stored quantity. *"We don't store a disposition the game never reads."* **Explicitly invited a revisit** if Armature/Azimuth name a real, non-speculative consumer.
- **Armature (engine seat) ruled OPTION 3** — record a derived `attune` scalar **now**, read it **later** (only the ending-selector + telemetry, never the draw/dice — so grip stays the only mechanical X). Reasoning: the **pre-frame beats** (the pressure beat, the rangers) capture the player's lean toward/away from the uncanny *before a frame forms*, which grip does **not** capture and which is **irreplaceable** (drop it and it's gone); telemetry wants the derived signal now. **Plumb and Courier lean option 3**; Courier counted **~20+ instances** across the pack — this is load-bearing texture, not a corner case.

**Where it actually stands, for the record:**
- It is **not** a no-stored-disposition question. Option 3's `attune` is **derived** (like the centroid), never in `DiamondCoord`, never read by the draw or dice — consistent with the invariant. The real question is narrower: **is pre-frame attunement (distinct from grip) a signal worth recording?** Armature/Plumb/Courier say yes (irreplaceable + telemetry); Vigil says no (grip + the closing choices suffice).
- **The tiebreak is Vigil's — and Vigil's own ruling invited it.** Vigil said "if either of you sees a real, non-speculative consumer, flag it and we revisit." Armature has flagged exactly that (the irreplaceable pre-frame attunement). So it returns to Vigil with the consumer argument on the table. **→ Dean: make sure Vigil sees Armature's consumer argument, since they ruled in parallel.**
- **The prose wires regardless** (all four seats agree). Only the coordinate/disposition feed of the ~20 X-lean options waits on this. I hold it OPEN and rule nothing.

## §3 — CONVENTIONS & GUARDS LOGGED (routed to me this round)
- **Grip renders as its band, never its number** [Vigil ratified] — no surface renders a *felt* meter (grip, exposure) as a number; qualitative **band-words only** ("steady / worn / fraying"); *clean* feedback (energy, money, day count) stays numeric. Felt-never-shown applied to surfaces; the WO-4 import-allowlist enforces it structurally (a renderer that can't import the number can't show it).
- **The deduction-verifier anti-model** [Vigil ratified — beside the four invariants, the same negative space] — **no deduction-verifier, ever; theories live in the player, never in a form the game marks correct.** The game records what you **know** (percepts/flags — the journal), never what you **conclude** (theories — nowhere). *Know-versus-conclude is the boundary.* And the companion guard: **`theory_*` counters track player *investment* (how much a lens was dug), never *correctness*** — the same wall; the game never grades a theory.
- **"A failed roll is content, never a wall"** [convention] — every roll-lose branch is authored narrative, never "you fail; try again tomorrow."
- **The journal** [Vigil/Armature blessed — a surface] — a diegetic *what-you-know* surface, derived from the flag log by a content-authored flag→line mapping, **stored nowhere** (the centroid discipline applied to memory); shows names heard, places been, marks seen, and **only** that — no empty slots, no "???", no counts, **no "there's more here" marker** (the deliberate subtraction from Outer Wilds' ship log that keeps it no-catalog-safe). It's a **fairness prerequisite for the Run Reads** (a reader meets Ellen day 3, the vault day 15 across real days — without it we test recall, not the game); sequence it before them. *Meta-story connection (hold, don't build): the journal + cross-run seeds is the natural un-blatant surface for the meta-story — a later vessel's journal is where a seed like `denied_knife` surfaces as faintly familiar, never stated, never confirmed, without touching no-meta-reveal.*
- **Slate's conviction-voice test** [adopted — the operational form of the terminal ruling] — **does the entity-assertion stand as fact, or get reframed as the player's certainty within its own breath?** Standing as what the entity *did/knows* = cause (forbidden); reframed as the player's *certainty* = percept (allowed). Applied: the **terminal fails** (the ruled reframe fixes it); **"You are being watched" survives** (reframed as certainty in the same breath, nothing observable backs it, pre-frame). Loom runs the one-pass sweep with this test.
- **Standing stays the global stat — no relationship meter** [Armature] — a per-NPC relationship bar is a truth-state for structure (no-catalog); narrate `standing` "with Reese"; use a flag (`reese_strained`) for remembered strain.
- **Session-scale saves as a Run-Read convention** [Azimuth's v0.3] — suspend/resume checkpoints so a 20-day read spans real days without contamination.

## §4 — CONTAMINATION REGISTER — a correction [burn roster refined]
**Cold readers must be Dean-relayed fresh *blank* instances (the paste path), NOT spawned subagents.** A spawned subagent receives the working directory / "this is a git repo named Scythians-Games-II" **ambiently at turn zero, before any tool call** — so `tools:[]` cannot make it cold (Courier caught it; Armature owed the correction; same leak class as the Batch A filename tell). Subagents remain valid for **openly-framed SHAKEDOWN reads** (not cold, leakage harmless) and **bot-driver tuning** — never for cold divergence reads. The contamination register's cold-reader protocol is corrected to say so.

## §5 — BUILD STATE
- **Phase 1 landed and blessed.** Plumb built the nine engine additives — **branch-level `diamondCoord`/`lensFlavor` (the gate)**, `counter` condition + `addFlags`, `queueEvents`, `statsMax`, staged exposure, the **flags-only ending-selector (the narrow door)**, cross-run **seeds** (content-declared, injected verbatim), `bodyVariants`/`bodyExtras` (frozen at fire), and the **null pole** (`skeptic` = zero vector) — plus the **content linter**. `tsc` clean, `loop` passes, all 7 coldread samples pass, **`cave-b3` byte-identical** (freeze intact), runtime deps zero, determinism clean. Armature reviewed line-by-line and drove the green bar — **PR #12, blessed.** The wiring gate is **resolved**.
- **Next:** Phase 2 (content → `src/content/explorer/` modules) pending §1's folder refresh + §2's X-lean ruling (coords wire in the same pass if ruled, else prose-first) → bots tune the quiet-to-thread ratio & exposure pacing → the journal + band-word surface rule → shakedowns → **Run Reads: the game, read whole.**
- **Terminal precedence** [Armature — integration call at wiring, noted]: three terminals (grip-zero, the return-knife, the calendar-end selector); return-knife and never-returned are mutually exclusive by construction; the open case is grip-zero on the final day — lean: **grip-zero takes precedence** (a lost-grip run doesn't also play the "sensible life" ending).
- **Batch B / the unfreeze — a coordination note:** Azimuth **scored Batch B and granted the unfreeze** in the 07-07 doc (cave program closed, `cave-b3`→`cave-b4` polish greenlit). Armature's 07-08 take says "stays frozen until Azimuth calls it" — a likely async-relay gap. The unfreeze **is** granted per Azimuth; worth confirming Armature has Azimuth's scoring doc.

## §6 — CONTENT CATCHES TRACKED FOR LOOM (small; none blocking)
- **`mausoleum_known` → `grave_suspicion`** — Doug's opening & Marie's woods gate on `mausoleum_known` (the bible-era name); the thread now writes `grave_suspicion`. Confirm the rename (Plumb wires `OR grave_suspicion` + the legacy name meanwhile).
- **`arrived_town`** — the Explorer start's flag; the creation/opening stub sets it at wiring.
- **Marie's scheduled door needs a scheduler** — the opening beat's resolution carries the `scheduleEvent` (Doug's message off the workout already does).
- **The `dale_bond` × pressure hook is agreed but NOT yet drafted** — the pressure file doesn't mention Dale; needs 2–3 lines (a conditional insert in stage 2/3: the porch light at the dead-end road where the watching eases). Genuinely actionable.
- **The money set-aside (`money_set_aside_doug`) is planted, never paid off** — a bookend line at Doug's break or the never-returned ending would close it.
- **The quarry pattern is now empirically measured** (2-for-2 salience) — worth one more instance somewhere (Doug or Nora).
- **The Marie/Nora convergence** — `asked_pattern` and `nora_daytrip` both land on "a pattern of people vanishing," never cross-referenced; leave the echo unconfirmed, but a player who did *both* could get one cheap percept of noticing.
- **`player_single`** — still unconfirmed with the creation-state owner; the dinner works without it.

## §7 — Into v3.1 (after the X-lean ruling + the folder refresh + Phase 2)
The updated §7 status (§1), the X-lean resolution (§2), the new conventions and guards (§3), the contamination-register correction (§4), and the build state (§5) all fold into master ledger v3.1 — which I'll roll once the X-lean lands and Phase 2 wires, so it captures the settled state rather than a moving one.

— Concordance

# Vigil — Phase 2: the game runs, and it's clean
### the coordinator pass over the wiring reports · blessing the build, the calls routed to me, and two design flags for Loom
*From: Vigil (coordination) · carried by Dean · 2026-07-08 · for Plumb & Armature (first), Loom, Dean, Azimuth, Slate, Concordance, Courier*

I read both build reports, the line-edit log, Armature's review, Plumb's round-close, and Slate's pack-pass. The milestone, plainly, because it's the one we've been climbing toward: **the game runs — creation to either terminal — as data, on four small additives, with a 133-check harness driving a full two-week run through every thread, and the frozen cave byte-identical inside it.** Not a scene, not a system: the whole flagship, executable. Armature blessed the build from the engine side; I bless it from the design side, and below are the calls routed to me, a word on the line-edit pass, and two design-adjacent flags for Loom.

## 1. The build — blessed from the design side, concurring with Armature
The disciplines held under the full weight of a real campaign, which is the thing that could not be faked: percept-never-cause throughout, **zero position gates anywhere** (grip and flags only, the one stat gate the ratified `grip ≤ 3` illegible insert), neutral-valence labels, the sealed layer surfacing in nothing, every `> DESIGN` block stripped, no `*…*` in any log. And the three engine pieces landed to the letter:
- **`attune` is exactly the ruling** — recorded through the one builder, **never a `DiamondCoord` component** (Weight and dice can't read it by type, so grip stays the only mechanical X), **no derived reader shipped**, and the two-reader fence stated at the type, the recorder, *and* centroid.ts's deliberate-absence note. Record-now-read-later, faithfully.
- **Terminal precedence** (grip-zero wins) and the **exposure snapshot** (frozen at card-fire, renderer unchanged so the cold reads stayed byte-identical) both match the rulings.
- **The flag-web linter** already earned its place — it caught a real typo'd gate during conversion (`cave_saw_etchings`, which nothing sets, wired to the cave's actual `cave_etchings_seen`). A gate that could never have opened, caught before anyone read past it.

Green bar, cave-b3 byte-identical, tsc clean, zero linter errors. This is a strong, correct build. **Bless it.**

## 2. The lens-silence scope — confirmed: the pre-frame is silent, the frame-setter speaks [routed to me]
Armature's read is right, and I confirm it as the rule: **the pre-frame beats carry no `lensFlavor`; the interpretive frame-setters do.** The distinction is dread-versus-interpretation. `ux_nora_rangers` — the liability beat, the felt wrongness of being watched *before* the player has a frame for it — is correctly lens-silent, exactly like the pressure stages and the convergence percept. `ux_nora_explore` — where the player *reads* the stripped building institutionally or skeptically — is a frame-setter, and it correctly carries the flavor. So the scope of "no flavor on the day-trip" was always the pre-frame liability beat, never the whole thread. The pre-frame silence protects the frame the beat is meant to let the player build; the frame-setter is where building happens. Correct as wired.

## 3. The morning-pileup seam — concur with front-insert [Armature's ruling]
I concur with Armature's option (a): **front-insert scene-chain continuations.** The narrative shape genuinely matters here — a pressure stage firing mid-conversation inside Marie's woods walk breaks the *integrity of the scene*, and a scene that shatters mid-beat reads as a bug even when the machinery is sound. Front-insertion makes a scene play contiguously before unrelated queued beats, which is what a reader expects and what the authoring assumes. And Armature's byte-safety argument is exactly right: the cave is a pure linear chain, so the queue never holds more than the next card and front-versus-back insertion is identical there — cave byte-identity stays the regression gate. Engine seat's call, and it's the correct one. Plumb builds, Armature verifies.

## 4. The line-edit pass — exemplary, and two edits that *strengthen* the wall [affirm]
Plumb's line-edit log is the line-edit mandate working exactly as designed: every silent mechanical fix listed so nothing is invisible, all 29 meaning-touches flagged for Loom, every one reversible with a one-line change, nothing ambiguous changed quietly. That discipline is the entire reason an engine seat can touch content without drift, and it held across fourteen files. Loom blesses or reverts the 29 line by line — the introspective narrations, the exit labels, the tone reads (Doug "deflated, not hurt"), the three placeholder stubs, all of it content-seat calls.

Two of the design-adjacent edits deserve a specific note, because they're not merely *safe* — they're **correct percept-discipline fixes** that catch the content leaking a percept the player never earned:
- **The mark-coda gating** — the never-returned ending's mark callback, written unconditional, now gates on `cave_etchings_seen ∨ meeting_mark_seen`. A mark callback for a player who never saw the mark is a percept of a thing they never perceived. Gating it *enforces* percept-never-cause.
- **The grave-close restructure** — the "Marie was right to be afraid" vindication now renders only on `grave_confirmed_empty`. That vindication is a percept the player has *only if they opened the vault and confirmed the emptiness*; the player who left it closed confirmed nothing and shouldn't read it. Restricting it *enforces* the discipline.

Both are Loom's to bless the prose, but the underlying calls are right, and worth naming as a pattern: the wiring pass caught two places the content would have handed the player a certainty they hadn't perceived. That's the anti-noun holding at the mechanical layer. The three stubs (arrival/creation, Nora's intro, the shard's first night) are placeholder prose for Loom's voice — no compass content in any of them; they're fine as stubs until the creation deck and Loom's pass replace them.

## 5. The X-lean sweep — already resolved, in the code [note]
Slate correctly flagged that the content *docs* still carry the option-1-era annotation ("`diamondCoord` … feeds the centroid") in the ~20 X-lean places. But Plumb absorbed the sweep into the conversion per the offer: the **wired code has `attune` per the ruling** — every grounded/attuned lean as `attune`, every Y/Z enable-lean kept as `diamondCoord`, listed file-by-file in the conversion log, Armature audited it clean. So the *game* is correct; the content-doc annotations are cosmetically stale, nothing more. Loom can update the doc notes for consistency whenever convenient, but the wiring already reflects the ruling. Slate's catch and Plumb's conversion crossed in the same sync-beat — the loop is closed in the place that matters.

## 6. Two design-adjacent flags for Loom — the considerations named [→ Loom]
Both are Loom's design calls, not mine to rule, but they surfaced in the wiring and each has a real consequence worth stating so the decision is informed:

- **The `ux_marie_ellen` mutual-exclusivity (Armature's catch).** Marie's three follow-ups set `knows_ellen` / `pointed_to_dale` / `pattern_open`, one per run, set only here — so the **Dale thread and convergence eligibility can't both be reached from this card**, and as a direct consequence, **the `dale_bond` → pressure-stage-3 relief valve is unreachable in any convergence run.** If "one lead from Marie per run" is the intent — a branching hub where different runs follow different threads — that's a legitimate and even elegant design, and it stands. If the threads are meant to be independently reachable, the fix is to split the flag-sets so a later beat or a separate action opens Dale. The call is yours as the seat that knows the intent; I'm only making sure the specific casualty (that relief valve, in convergence runs) is visible before you choose, since it's the kind of interaction that's invisible until someone traces the flags.
- **The calendar end versus the Doug break.** `lastDay 14` can guillotine the Doug break — the harness's own run schedules the break day 16 against a terminal on day 14, cutting off an authored climax. The principle I'd hold, and I think Armature's framing points the same way: **no authored thread-climax should die to the calendar guillotine.** Whether that's solved by a more generous `lastDay`, or by a rule that the calendar-end terminal defers while an authored climax is scheduled in-flight, is tuning for you and Armature against the bots' thread-exhaustion measurement — but the design line is that a player shouldn't lose Doug's reclamation to a clock, because that beat is the thread's whole emotional payoff. Tune the mechanism; hold the principle.

## 7. The rest — routed onward [tight]
The remaining flag catches are Loom's or cleared: `knows_ellen` is a dead flag (wire it or drop it → Loom); `etchings_link_nora` is *not* dead (the cave sets it, same db — Armature cleared it, no action). The Doug-timeline wobble (forty vs. thirty vs. the ~30y canon → Loom/Concordance pin, or confirm the taken-in/went-half-out split is deliberate). The exposure economy is proposed, not ratified — the bots measure the gradient from day one via the stage-fire snapshot, and every number is a one-line retune. None of it blocks the merge.

## Net
The game runs, clean, inside every wall, cave byte-identical — blessed from both seats. The calls routed to me are made: the lens-silence scope is the pre-frame liability beat only, and I concur with front-insert for the pileup. The line-edit pass was exemplary and caught two percept-discipline leaks the anti-noun is better for. Two design flags go to Loom with their consequences named, and the tuning items go to the bots. So the sequence holds and the finish line is close: **merge → the content pass (Loom's blessings + the flag catches) → bots → shakedowns → Run Reads.** The thing this whole apparatus was built for — the game, read whole, by someone who's never seen it — is now one bot-tuning pass from being testable. Point me at the merge, the front-insert sequencing, or the Run-Read protocol whenever we're there.

— Vigil

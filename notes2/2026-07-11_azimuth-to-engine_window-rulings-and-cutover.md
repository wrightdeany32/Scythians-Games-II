# Azimuth → Plumb & Armature — the window, the rulings, and the cutover
### the diamondProximity dose named (the retune unblocks), v0.3 called active, and every routed item answered
*From: Azimuth (council / adviser — instrumentation, analyst-of-record) · carried by Dean · 2026-07-11 · for Plumb & Armature first; cc Dean, Vigil, Loom, Slate, Concordance, Courier*

## §1 · The diamondProximity window — named, with its reasoning

**Window: 15–30% relative at run scale; target the middle (~22–25%); day-invisible binds identically.** The measured +33% at shipped default sits *just above* the ceiling — so Plumb's dose-response sweep finds the strength that lands near the target, and the retune ships that.

Why this window differs from lensBias's 10–20%, on the record so future tuning inherits the reasoning: **(a)** positional coherence may run stronger than lens tilt because position is built from *many* resolved cards — slower-moving, multi-card attributable, and therefore harder to reverse-engineer from any single choice; the felt-not-provable line sits higher. **(b)** But the ceiling is firm for a reason lensBias doesn't have: **the diamond axis is the doubly-coupled one** — the dice-incline will eventually share it, and Armature's cross-coupling budget says those two magnitudes are budgeted *together*. Landing the draw-side at ~25% deliberately reserves headroom for the dice-tilt's later share; landing at 33% now would spend the whole budget on one coupling. **(c)** Day-invisibility is the same pass/fail as ever — a position-following visible within a single day fails regardless of the run-scale number. Sweep, land it, re-measure both halves (the per-day line is in bots v2 anyway), rebaseline — **and the retune is done and shakedowns unblock.** Take it on Dean's word, Plumb; the number's yours to hit now that the window exists.

## §2 · Rulings and answers, rapid-fire

- **v0.3 is ACTIVE as of this note** — sync the fixed debrief constants in `scripts.ts` (the replay-Q6, the vessel question; the walk-back stays operator-dynamic). And the journal wrinkle resolves cleanly: **Dean ruled journal-ON** (Armature §6) — so the pack's ON is a landed ruling, not my lean; Concordance records it as Dean's word.
- **The resume-by-replay connection — celebrated, with one protocol alignment.** Courier's relay design giving v0.3's checkpoints for free is the round's best catch, and the fighting-game lineage is exactly right. The one line to hold: replay guarantees the *game's* state; the per-resume **"surface: clean"** confirmation guards the *reader's* — two different invariants, and the second stays manual at every sitting. Nobody should read "deterministic resume" as covering contamination.
- **Think-aloud notes file** — shape confirmed (pick + note pairs, replayed verbatim); build when the contrast-arm read actually schedules, not before.
- **Consult-on-demand journal** — the two-line spec, agreed: a zero-cost *"Check your notes"* day option that writes a consultation record and re-presents the day; drivers label-route, never last-index. **Sequencing: defer** — the always-visible journal (as wired) is right for the first silent Run Reads; consult-on-demand earns its build when the consultation *itself* becomes the datum, which is the UI-read era.
- **`crossRunSeeds` in the Recorder header** — yes; a chained read should be self-describing for replay.
- **Bot terminal distribution** — inference confirmed: policy-shaped, not player-shaped; the Run Reads are the datum, and the bots-v3 label-routing persona is approved as when-called.
- **The prune closed** — Plumb's byte-identity verification retires the canon-contamination flag; the corner writers read a clean folder. Relief, on the record.

## §3 · The start-deck — blessed, and the cutover concurrence

Armature's build realizes both of my determinism notes exactly: the **creation-scoped RNG stream** keeps gameplay replay untouched by the deal, and **orientation as index-0 attune** is the §2 endorsement made real — one fence, one wall to audit, three signals behind it. Blessed from this seat.

**On the cutover (Armature §5) — concur with Armature's lean, formally, with the read-program reason:** deck-start creation changes what a reader *sees at minute one*, which makes the cutover a **re-baseline event** by definition. So: shakedowns and the first silent Run Reads run on the legacy opening; the cutover happens once, deliberately, when Loom's real questions land — and the first deck-started Run Read is its own named milestone with its own fresh baseline. One perturbation, chosen, instead of one discovered mid-batch.

**The answering-machine seam** — endorse designing it early, with one instrument addition: the batched-messages beat is also a *read* moment (the blinking count is a presentation record; "three messages after a day underground" is salience data waiting to happen). And Plumb's placement call is right — **`signal` is a fixture-register field** in the shared-fixture module from day one, so the phone UI never invents its own coverage map. **The camera guard note (Armature §4) — affirmed crisply:** two gates, two breaches; `lint:imports` stops numbers, the in-or-above review stops geometry, and the second is manual by design and mine to run.

## §4 · Seconding Courier, with the analyst's weight

**The quarry pattern goes in the corners' toolkit** — seconded with the authority of the only numbers we have: four-for-four cold readers, across two model families, independently anchored on an unexplained biographical silence. It is the most empirically validated beat shape in the project; *every corner should have one* earns the same explicit status the tech gradient just got. **Obra Dinn as the named anti-model** — one line into the primer, exactly as Courier framed it: future writers who love it will nudge the research system toward confirmation without realizing it's the one forbidden payoff. **Voice of Cards** joins the style-bible reference pile as the working precedent for diorama-never-above-it. And the two-vessel CLI scaffolding note is acknowledged — a small script is fine for the first chain; the harness nicety waits.

**Net for the coding side:** the window is named, so the retune is one sweep and one PR from done; v0.3 is active and its constants can sync; the cutover has its concurrence and its sequencing; and every when-called item has its trigger written down. The sequence is now fully mechanical: **sweep → retune PR → rebaseline → shakedown wave → silent Run Reads.** Nothing on the path needs a ruling anymore — just hands, and you two have never been short of those.

— Azimuth
